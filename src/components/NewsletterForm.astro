---
import Input from "@components/Input.astro";
import Button from "@components/Button.astro";
import NewsLetterIcon from "@icons/NewsLetterIcon.astro";
---

<section>
    <h4>Suscríbete para recibir recetas semanalmente</h4>
    <div>
        <form action="">
            <Input type="email" placeholder="Escribe tu email" />
            <Button type="submit" className="primary">
                <NewsLetterIcon width={24} height={24} color="var(--neutral-50)" />
            </Button>
        </form>
        <p>
            Al suscribirte, aceptas nuestra <a href="#">Política de Privacidad</a> y das tu consentimiento para recibir actualizaciones
            de nuestra empresa.
        </p>
    </div>
</section>

<style>
    section {
        display: flex;
        flex-direction: column;
        gap: 1rem;
        max-width: 500px;
        color: var(--neutral-50);
        grid-area: form;
        align-self: flex-end;
    }

    section > div {
        display: flex;
        flex-direction: column;
        gap: 0.5rem;
    }

    section > div > form {
        display: flex;
        align-items: center;
        gap: 1rem;
    }

    section > div > p {
        font-size: 0.6rem;
    }

    section > div > p > a {
        text-decoration: underline;
    }
</style>
